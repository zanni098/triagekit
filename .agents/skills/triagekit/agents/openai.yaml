interface:
  display_name: "Triagekit"
  short_description: "Repo-specific patterns and workflows for triagekit"
  default_prompt: "Use the triagekit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true